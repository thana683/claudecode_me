# Task List — Thai Food Recommender

Derived from [context.md](./context.md). ดูรายละเอียด implementation ที่ `index.html` (ไฟล์เดียว รวมทุกอย่างไว้ที่นี่ — `thai-food-recommender.html` ที่เคยเป็นไฟล์ fragment คู่กันถูกลบทิ้งแล้วเพื่อไม่ให้ต้อง sync สองที่).

## สุ่มเมนู (Random Roll)

- [x] ปุ่มสุ่มเลือกเมนูอาหารไทยแบบสุ่มจาก dish list ทุกครั้งที่กด (ไม่ยึดวันที่)
- [x] กันไม่ให้สุ่มได้เมนูเดิมซ้ำติดกัน
- [x] auto-roll เมนูให้ทันทีตอนโหลดหน้าครั้งแรก
- [ ] ทดสอบพฤติกรรมสุ่มด้วยมือใน browser จริง (กดปุ่มซ้ำหลายครั้ง เช็คว่าไม่มีเมนูซ้ำติดกันจริง)

## เมนูที่สุ่มได้ (Rolled Dish)

- [x] แสดงชื่อเมนู + คำอธิบายสั้น 1 ประโยค + emoji
- [x] dish list มีข้อมูลครบ 35 เมนู (คาว/แกง/ผัด/ยำ/ของหวาน) พร้อม field `category` ต่อเมนู
- [x] แต่ละเมนูมี field `img` ชี้ไปที่ `image/<slug>.png` — ใช้แสดงแทน emoji เมื่อมีไฟล์รูปจริง (fallback กลับเป็น emoji อัตโนมัติถ้าโหลดรูปไม่สำเร็จ)
- [ ] รอ Codex สร้างรูปภาพ 35 เมนูให้ครบ (ติด usage limit ของ ChatGPT plan อยู่ ณ ตอนนี้ ลองใหม่ได้หลัง 29 ก.ย. 2026 หรืออัปเกรด Plus) แล้วเอาไฟล์มาใส่ folder `image/`
- [ ] รีวิวความถูกต้อง/ความหลากหลายของรายชื่อเมนูอีกครั้ง (เผื่อเพิ่ม/ตัดเมนูภายหลัง)

## รายการโปรด (Favorites List)

- [x] ปุ่ม ♥ เป็น toggle เพิ่ม/เอาออกจากรายการโปรด
- [x] กันเมนูซ้ำในรายการโปรดด้วยชื่อเมนูเป็น key
- [x] เก็บรายการโปรดผ่าน localStorage (คงอยู่แม้ปิด-เปิดหน้าใหม่)
- [x] แต่ละรายการมีปุ่ม × ลบออกจากรายการโปรดได้
- [x] เรียงรายการโปรดจากรายการที่เพิ่งกดล่าสุดอยู่บนสุด
- [x] empty state เมื่อยังไม่มีเมนูโปรด
- [x] ช่องค้นหาเมนูโปรดด้วยชื่อ (real-time filter)
- [x] ตัวกรองรายการโปรดตามหมวดหมู่ (คาว/แกง/ผัด/ยำ/ของหวาน) — ตัวกรอง/ช่องค้นหาแสดงเฉพาะตอนมีเมนูโปรดอย่างน้อย 1 รายการ พร้อม empty state แยกกรณี "ไม่มีเมนูโปรดเลย" กับ "มีแต่กรองแล้วไม่เจอ"
- [ ] ทดสอบ localStorage ใน private/incognito window (กรณี storage ถูกบล็อกหรือว่างเปล่า)

## หน้ารายละเอียดเมนู (Dish Detail Modal)

- [x] modal popup แสดงชื่อเมนู หมวดหมู่ คำอธิบาย และรายการส่วนประกอบ (แต่ละเมนูมี field `ingredients` ครบ 35 เมนู)
- [x] เปิดได้จาก medallion/ชื่อเมนูบน stage หลัก และจากแต่ละรายการในรายการโปรด (คลิกทั้งแถว ยกเว้นปุ่ม ×)
- [x] ปิดได้ 3 ทาง: ปุ่ม × ในมุมขวาบน, คลิก backdrop, กด Escape
- [x] keyboard-accessible: medallion เป็น `<button>`, แถวโปรดมี tabindex/role="button"/Enter-Space, โฟกัสกลับไปที่ปุ่มเดิมหลังปิด modal
- [ ] ทดสอบ modal ด้วยมือใน browser จริง (คลิกทุก entry point, ปิดทุกทาง, เช็ค scroll เมื่อ ingredients เยอะ)

## ดีไซน์

- [x] Redesign ตาม `.claude/DESIGN.md` (dark-void theme): พื้นดำล้วน #000, accent ม่วง Electric Iris #8052ff (ปุ่มหลัก), amber Saffron Spark #ffb829 (highlight/hover), ไม่มี card/border/shadow — ใช้ whitespace แทน, ปุ่มเป็น pill radius เต็ม (2026-09-20)
- [x] responsive ที่ความกว้างมือถือ (~400px)
- [ ] เช็คความคมชัดของสี (contrast) ของธีมใหม่อีกครั้งบนอุปกรณ์จริง

> **หมายเหตุมติเก่า:** เดิมใช้โทนสีอบอุ่นแบบไทย (แดงลาคเกอร์ / ทอง / ครีม) พร้อม light/dark theme ตาม system preference — ถูกแทนที่ด้วย dark-void theme เดียว (ไม่มี light mode) ตามคำขอ redesign อ้างอิง `.claude/DESIGN.md` เมื่อ 2026-09-20

## งานที่ยังไม่ได้ทำ (นอกขอบเขตเดิม แต่อาจพิจารณาภายหลัง)

- [ ] เพิ่มปุ่ม "ล้างรายการโปรดทั้งหมด"
- [ ] แชร์ผลลัพธ์เมนูที่สุ่มได้ (เช่น copy ข้อความ)

> **หมายเหตุมติเก่า:** ก่อนหน้านี้เคยตัดสินใจไว้ (อ้างอิง "Q9" ใน context.md) ว่า **จะไม่ทำ** ตัวกรองตามหมวดหมู่อาหาร — มติดังกล่าวถูก**ยกเลิกแล้ว** ตามคำขอของผู้ใช้ (2026-09-20) และได้ implement ตัวกรองหมวดหมู่ไปแล้วในส่วน "รายการโปรด" ด้านบน context.md ฉบับปัจจุบันไม่มีรายละเอียด Q9 หลงเหลืออยู่แล้ว จึงไม่ทราบเหตุผลเดิมที่เคยตัดสินใจไม่ทำ
